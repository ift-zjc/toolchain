
server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/toolchain?useSSL=false
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy


zjc:
  kafka:
    bootstrap-servers: dev.ift:9092
    topic: test-topic-1
    consumer:
      group-id: test-consumer-group
